[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 300
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E501",   # line too long -- managed per file
]
